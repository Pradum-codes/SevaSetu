# Admin Portal Environment Configuration

# API Base URL (when not using Vite proxy)
# During development with Vite proxy, this is not needed
# During production, this should point to your backend server
VITE_API_URL=http://localhost:3000
